const Discord = require('discord.js')
const log = require('../functions/log')
const ytdl = require('ytdl-core')

module.exports = {
    "name": "badapple",
    "description": "Play KeyFM",

    async execute(message, args, client) {
        message.reply("please for god's sake, stop.")
    }
}
